package main

// This package contains benchmark/report generator utilities.
// Individual generators may be excluded from normal builds via build tags.

// main keeps the benchmark source buildable as a standalone package. The
// individual benchmark functions are invoked by external harnesses.
func main() {}
