// Package ir is reserved for future extraction of root IR compiler/executor code.
//
// The neutral IR model already lives in core/internal/ir. This package is the
// eventual public/package-level extraction target for root IR compilation and
// execution code once runtime object, call, error, and frame contracts are
// explicit enough to break the current root-core coupling.
package ir
